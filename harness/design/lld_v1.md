# LLD · methodology_v3(分阶段实现规格)

> 来源:design-Q lld 阶段 W00(2026-07-29,14/14 全采纳)。
> 上游:[VISION](VISION.md) + [HLD](hld_v1.md) + [ADR-0004](../../harness/adr/0004-methodology-v3-hallucination-thesis.md)。
> 本文件 = v3 实现规格,交实现期(long-running-agent)按 P1–P4 执行。

## L1 阶段拆分(4 阶段,各自独立可验证)

| 阶段 | 内容 | 独立 DoD |
|---|---|---|
| **P1 主文立论重写** | §零 / §1 / §2(双靶子 / 两类断层 / 机制层) | §2.1 四必含(L3 #10) |
| **P2 立论挂接** | §5 / §6 / §7(问答 / AI 替代角色 / 过载挂到新立论) | 挂接点无断链 |
| **P3 CONTEXT 同步** | 四术语分层 + 第一支柱标准句 | CONTEXT 与 v3 用词一致 |
| **P4 README/CLAUDE 同步 + 发布** | 标准句复用 + 发布六步 | 全局 DoD(L4 #12)全绿 |

## L2 详细设计(章节级改写规格)

### §2.1 第一支柱(P1 核心)
1. 新增**机制层小节**,以 DoD-1 因果句开篇:「背景缺失 → AI 在信息真空幻觉式自作主张 → 问答对齐是对抗此机制的直接手段」
2. 紧接**三层立论模型表**(机制层 / 度量层 / 症状层)
3. 保留原「有效上下文 120k / 400k」作度量层
4. 末尾引用 §7.6 过载对策

### §1.2 两类信息断层(P1)
改写为两类并列:
- **人与人断层**(保留原「等 PM / 沟通不一致」例证,挂 §6.2 AI 替代角色)
- **人与 AI 断层**(背景缺失 → 幻觉,挂 §5 问答对齐)
- 返工 = 两类断层的共同症状

### §5.1 (a)(b) 盲区(P2)
补「(a)(b) 盲区」小节:(a) 知道没写 → preview / Q 机制捕获;(b) 自己也不知道 → grill 对抗逼出。原「不替决策 / 帮看清决策」原则保留作该节引子。

### §零 / §1 双靶子(P1)
§零(适用场景)加 vibe coding 为首要痛点;§1 开篇换 vibe coding 痛点引子,§1.1「等 PM/QA」降为「传统多人流程对照」子场景。

### §7.6 过载对策链(P2)
补「会话内过载对策链」小节:design-Q 等 skill 沉淀决策落盘 → /compact 压缩。

### v3 文件头(P1 起)
加「v3 相对 v2 演进」引言,记推导链:本意(vibe coding 幻觉)→ v2 写作中工程化措辞替换 → 立论偏离 → grill-Q W01 抓出 → v3 补全机制层。

### v2 文件头(P4)
加一行「历史版本,以 v3 为 current」标注(不删内容)。

### CONTEXT(P3)
「双支柱」节扩写:第一支柱换 [HLD #11](hld_v1.md) 标准措辞句 + 新增四术语分层表。

## L3 接口规格(硬约束)

- **§2.1 四必含**:① DoD-1 因果句 ② 三层立论模型表 ③ 120k/400k 度量 ④ §7.6 引用——缺一不算 P1 完成。
- **同步硬约束**:README / CLAUDE 第一支柱句 = HLD #11 标准句**原样复用**;CONTEXT 四术语条目格式统一 =「术语 | 层级 | 一句定义」。

## L4 DoD(可脚本化)

### 全局 DoD(实现期跑)
1. **锚点检查**——v3 大标题集合 = v2 大标题集合(grep 比对)
2. **标准措辞四处一致**——grep 第一支柱标准句出现在 v3 §2.1 + CONTEXT + README + CLAUDE
3. **[scripts/desensitize.py](../../scripts/desensitize.py) 0 命中**
4. **四术语用法一致**——grep 四术语在 v3 + CONTEXT 定义层级行为一致

### 回归 DoD
v2 既有非立论章节(§4 工作流 / §6 分工 / §7 信息管理等)**结构编号不变**,仅措辞同步——不破坏现有功能。

## L5 依赖与预估

- **依赖**:ADR-0004 / VISION / HLD 已定,无阻塞;纯文档项目无外部依赖(不需环境实测)。
- **工作量**:P1 主文重写最重(§2.1 / §1.2 / §5.1 是重头)→ P2 挂接 → P3 / P4 同步递减。
- **实现期建议**:用 long-running-agent 驱动(跨会话),feature_list 从本文件 P1–P4 反推。
