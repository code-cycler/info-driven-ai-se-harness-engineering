---
mode: feature
wave: 1
stage: lld
created: 2026-08-06
status: processed
---
# 问卷 lld W01 · L2 详细设计五块改写草案

> **与 W00 的关系**:W00 全采纳(10/10)。L2 详细设计(要点 10 预告)→ 本波 Q1–Q5 出五块改写草案。其余 L1/L3/L4/L5 已定。
>
> **填写规则**:
>
> 1. 每题勾选 `[x]`;★ 推荐;opt-in 关不预勾
> 2. 🤔 逃生舱:勾了 = 定不了 → 降风险协议
> 3. **每题给默认草案**:A = 采纳草案;✍️ = 指出改哪句/改哪个格式
> 4. 草案是「将写进 skill 文件的最终文本」,认可即落 LLD 规格

## Q1. STAGE-SKELETONS.md 头部「HLD/LLD 判别法则」节最终文本   [落盘: LLD#L2 + STAGE-SKELETONS.md]

<出题依据:hld W01 Q2 定稿判别法则表述。本节放 STAGE-SKELETONS.md「使用方式」之后、「Stage: vision」之前。hld_v1 越界(章节级改写混进 HLD)是判别缺失实证。>

**草案**:

```markdown
## HLD 与 LLD 的职责区分(判别法则)

> hld / lld 两阶段易混淆越界(实证:hld_v1 把章节级改写混进 HLD)。出题与落盘前,先用判别法则判定每条内容属 HLD 还是 LLD。

**HLD = 架构骨架(phase-invariant)**:回答「系统长什么样、各部分如何衔接」,跨所有实现阶段稳定。= 模块/区边界与职责 + 全局选型(含被否决项)+ 模块间契约 + 部署运维 + ADR 识别。

**LLD = 构建路径(incremental)**:回答「分几步建、每步具体做什么、何时算完」,随实现推进而细化。= 阶段拆分(依赖链)+ 每阶段详细设计 + 函数级接口规格 + 每阶段 DoD + 依赖与预估。

**判别问句**:① 「这条内容会随阶段拆分变化吗?」会 → LLD;否(系统级稳定事实)→ HLD。② 「它是模块边界,还是模块内部?」边界 → HLD;内部 → LLD。

> HLD 不写「怎么逐步实现」(那是 LLD 的阶段拆分 + 详细设计);LLD 不重写「系统由什么组成」(那是 HLD 的架构)。两阶段职责不重叠。
```

- [X]  A. 采纳草案 ★推荐 —— 定义 + 两句问句 + 职责不重叠声明,可操作
- [ ]  B. 删最后一段「HLD 不写…」(只留定义 + 问句,更简)
- [ ]  🤔 我定不了 → 推迟/降风险

- ✍️ 改哪句: __________

## Q2. 每项「最小必含 + 产出形态」标注格式(怎么附在现有骨架表)   [落盘: LLD#L2 + STAGE-SKELETONS.md]

<出题依据:现有骨架是 markdown 表格(# | 必问项 | 出题方向 | 落盘 | 必答)。最小必含子项有 a/b/c,表格单元格装不下且难读。hld W01 Q3 定稿的 10 项最小必含要落地成可核对的子项清单。>

- [X]  A. **表格保留作总览 + 每项下加子块**(推荐格式见下) ★推荐 —— 表格作索引,子块装最小必含 + 产出形态,各得其所
- [ ]  B. 表格加两列(最小必含 | 产出形态) —— 紧凑,但表格过宽且子项 a/b/c 在单元格里难读
- [ ]  C. 表格不动,另起一节「最小必含 + 产出形态速查」 —— 分离,但查两项要跳读两处
- [ ]  🤔 我定不了 → 推迟/降风险

- ✍️ 自定义格式: __________

**A 方案格式示例**(以 H1 为例):

```markdown
## Stage: hld

| # | 必问项 | 出题方向 | 落盘 | 必答 |
|---|---|---|---|---|
| H1 | 系统架构 | 模块划分、职责边界、数据流向 | HLD#架构 | ✅ |
| H2 | 技术选型 | 技术栈/库/协议 + 为什么;被否决项也要记录 | HLD#选型 + ADR | ✅ |
| ... | | | | |

### 最小必含 + 产出形态(hld 阶段各项)

**H1 系统架构** · 产出形态:模块表 或 架构图
- 最小必含:① 模块/区划分清单 ② 每模块职责一句话边界 ③ 数据/控制流向(图或文字链路)

**H2 技术选型** · 产出形态:选型表,含被否决列
- 最小必含:① 选型项 ② 选它的理由 ③ 被否决项(至少 1 个)
(…… H3–H5 同构)
```

## Q3. STAGE-SKELETONS.md「坍缩分档」节最终文本   [落盘: LLD#L2 + STAGE-SKELETONS.md]

<出题依据:现有「阶段坍缩(可选)」节(D17/D19)。hld W01 Q5 定档:完整档全量 / 坍缩 hld 档只 hld 五项 / 坍缩 lld 档只 lld 五项 + H2/H5 实质并入。要落成含最小必含的分档表。>

**草案**(替换现有「阶段坍缩(可选)」节):

```markdown
## 阶段坍缩与最小必含分档

默认 vision → hld → lld 三阶段(完整档,全量最小必含)。定模时按「经验 × 规模」给坍缩建议,用户确认。

| 档 | 阶段 | 最小必含(防简化) |
|---|---|---|
| **完整档** | vision → hld → lld | H1–H5 + L1–L5 全量 |
| **坍缩 hld 档**(没经验) | vision → hld | 只 hld 五项(H1–H5);lld 不预先写,实现中边做边调,沉淀的详细决策即时落盘 |
| **坍缩 lld 档**(小项目:skill/脚本/小工具) | vision → lld | 只 lld 五项(L1–L5);H2 选型 / H5 ADR 若有实质内容,并入 lld 文档或单独 ADR |

> 各档保留该档必需的最小必含约束——坍缩档**不免除**最小必含(否则回退 hld_v1 简化状况)。
> 判据来源:D19(2026-07-23 retro hld 闸门用户指示)。
```

- [X]  A. 采纳草案 ★推荐 —— 分档表 + 「不免除最小必含」声明 + D19 出处
- [ ]  B. 保留现有节文本,只追加最小必含列 —— 改动小,但现有节是散文非表格,追加难融合
- [ ]  🤔 我定不了 → 推迟/降风险

- ✍️ 改哪句: __________

## Q4. SKILL.md 第 2/4 步路径决定逻辑改写(方案 R 落点)   [落盘: LLD#L2 + SKILL.md]

<出题依据:HLD H1.2 路径决定流。现有第 2 步硬编码 `docs/questionnaires/`(SKILL.md:54),第 4 步硬编码 `docs/adr/`(SKILL.md:67)。改写为配置化根 + 声明识别 + 确认。>

**草案**(第 2 步「生成问卷」内新增路径决定子步,替换硬编码路径):

```markdown
- **确定落盘根**(方案 R:声明优先 + 默认兜底 + 落盘前确认):
  1. 读项目 CLAUDE.md,匹配声明关键词〔「落盘根 / 落盘速查 / harness 区 / 设计产物落 / harness 文件」〕→ 命中取该节指示的根(权威)
  2. 未命中 → 默认 `harness/`(懒创建)
  3. AskUserQuestion 确认根路径(首次或项目结构变化时);后续波次沿用,处理报告标注「根 = X,沿用首次确认」
- 写到 `<落盘根>/questionnaires/<stage>-w<NN>.md`(feature 模式:`feature-<slug>-<stage>-w<NN>.md`),status: pending。目录懒创建。
```

**草案**(第 4 步「处理与落盘」落盘句改写):

```markdown
- 逐题落盘到 `<落盘根>/` 下:阶段文档 → `<落盘根>/design/`(VISION/HLD/LLD);ADR → `<落盘根>/adr/NNNN-<slug>.md`;**CONTEXT.md / OPEN-DECISIONS.md / TODO.md 为项目固有文件,路径不动**(各项目已定型)。本波处理完即刻写。
```

- [X]  A. 采纳两段草案 ★推荐 —— 声明→默认→确认三步 + 项目固有文件路径不动
- [ ]  B. 第 2 步确认改为「每波都确认」(不沿用) —— 更安全但退化成一问一答
- [ ]  🤔 我定不了 → 推迟/降风险

- ✍️ 改哪句: __________

## Q5. PROCESSING-RULES.md 落盘映射表新格式(配置化根)   [落盘: LLD#L2 + PROCESSING-RULES.md]

<出题依据:HLD H2 方案 R。现有落盘映射表硬编码 docs/design/、docs/adr/(PROCESSING-RULES.md:28–37)。改写为 `<根>` 占位 + 项目固有文件标注。>

**草案**(替换现有「落盘映射」表):

```markdown
## 落盘映射

所有落盘**本波处理完即刻执行**,不批处理。目标文件不存在时懒创建;命名沿用项目既有约定(如 `_vN` 版本规则)。

**落盘根 `<根>`** 由 design-Q 主流程第 2 步确定(方案 R:CLAUDE.md 声明优先 → 默认 `harness/` → 落盘前确认)。`<根>` = 已确认的落盘根路径。

| 答案类型 | 落盘目标 |
|---|---|
| vision 阶段题 | `<根>/design/` 的 VISION.md |
| hld 阶段题 | `<根>/design/` 的 HLD 文档 |
| lld 阶段题 | `<根>/design/` 的 LLD 文档 |
| 术语定义类 | CONTEXT.md(项目固有,路径不动) |
| 满足 ADR 三条件的决策 | `<根>/adr/NNNN-<slug>.md`(编号顺延现有) |
| 🤔 逃生舱(单向门) | OPEN-DECISIONS.md(项目固有,路径不动) |
| 🤔 逃生舱(双向门) | 采用 ★推荐项 **且进 OD 标注**(标「双向门 / 采用推荐项 X / provisional」+ 重访触发条件) |
| 行动项 | `<项目根>/TODO.md`(项目固有,路径不动;懒创建) |

ADR 三条件(缺一不写):难逆转 + 缺上下文会让人困惑 + 经过真实权衡。
```

- [X]  A. 采纳草案 ★推荐 —— `<根>` 占位 + 项目固有文件标注 + 双向门逃生舱留痕保留
- [ ]  B. 逃生舱双向门那行删「且进 OD 标注」(回退到不进 OD) —— 与 D22 决策冲突,不推荐
- [ ]  🤔 我定不了 → 推迟/降风险

- ✍️ 改哪句: __________

---

## 补充声明

<任何想补充的话:新需求、格式反馈、范围调整、临时想到的风险……没有就留空。agent 处理时必读>

---

## 处理报告摘要(2026-08-06 · lld W01 → processed)

- **Q1**:[x] **A 采纳**(判别法则节最终文本:定义 + 两句问句 + 职责不重叠声明)
- **Q2**:[x] **A 采纳**(标注格式:表格保留作总览 + 每项下加最小必含/产出形态子块)
- **Q3**:[x] **A 采纳**(坍缩分档节:三档表 + 「坍缩档不免除最小必含」+ D19 出处)
- **Q4**:[x] **A 采纳**(SKILL 第 2/4 步路径决定:声明→默认→确认 + 项目固有文件路径不动)
- **Q5**:[x] **A 采纳**(PROCESSING-RULES 落盘映射表:`<根>` 占位 + 项目固有文件标注 + 双向门逃生舱留痕保留)
- **opt-in 关,无取消率;无逃生舱;无 ✍️ 改动**
- **LLD.md 已落** `harness/design/skill-spec-revamp/LLD.md`(含 L1–L5 最小必含 + 五块改写文本 + 自检 dogfood 通过)
- **lld 阶段骨架 L1–L5 全覆盖,动态盲点清零 → 设计阶段完成**
