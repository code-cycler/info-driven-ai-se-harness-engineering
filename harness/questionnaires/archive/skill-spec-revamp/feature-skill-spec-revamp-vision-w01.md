---
mode: feature
wave: 1
stage: vision
created: 2026-08-06
status: processed
---
# 问卷 vision W01 · 落盘路径配置化机制 + 全局生效处置

> **与 W00 的关系**:W00 留空(不采纳)2 条 → 第 3 条(落盘路径配置化)、第 13 条(全局生效处置),作为正式题深究。其余 13 条已采纳,W01 不重出。
>
> **填写规则**:
>
> 1. 每题勾选 `[x]`;默认单选,标「(多选)」可勾多个
> 2. ★ = 推荐选项,附推荐理由;opt-in 开关未启用,不预勾
> 3. 每题末尾 🤔 是逃生舱:勾了 = 定不了 → agent 走降风险协议,绝不重问
> 4. 选项都不合适 → ✍️ 自定义自由书写
> 5. 标「条件:Qn 选 X 才答」的是内联浅分支,条件不满足直接跳过
> 6. 选项排序 = 非推荐在前 → 🤔 倒数第二 → 推荐最后

## Q1. 落盘路径配置化机制(改动 1 核心)   [落盘: VISION#范围 + HLD#文档机制细化]

<出题依据:探索确认四 skill 硬编码 `docs/`(design-Q 3 处 / grill-Q 3 处 / retro-Q 3 处 / action-Q 4 处);本仓库 CLAUDE.md「落盘路径速查表」已写 harness/ 路径(harness/questionnaires/archive/、harness/adr/ 等),即项目实践先行、skill 规格滞后;项目A 用 docs/。skill 是通用工具(~/.claude/skills/,服务所有项目),不能硬编码任一目录。W00 第 3 条默认「声明优先+探测兜底+确认」未采纳,故出全方案对照。>

- [ ]  A. 固定 `docs/`(skill 不改,项目自己适配) —— 痛点不解决:本仓库每次 skill 落 `docs/` 还得手动挪到 `harness/`;但 skill 实现最简单
- [ ]  B. 纯探测(看 `harness/` 目录存在与否,存在用 harness/ 否则 docs/) —— 实现简单;但「魔法」——项目有 `harness/` 却不放设计产物时误判,且用户看不出 skill 怎么决定的
- [ ]  C. 纯声明(只认项目 CLAUDE.md 显式声明,无声明则问人/报错,不探测) —— 最可预测;但新项目每次先写声明有负担,与 skill「懒创建、低门槛」气质冲突
- [ ]  D. 每次落盘前问人(「这次问卷落哪?」) —— 最安全;但违背批量效率,每波都问 = 退化成一问一答
- [ ]  🤔 我定不了 → 推迟/降风险
- [ ]  E. 声明优先 + 探测兜底 + 落盘前确认 ★推荐 —— 三道防线:① 项目 CLAUDE.md 有声明用声明(权威);② 无声明探测 `harness/` 存在(兜底);③ 落盘前显式确认路径(防误落)。兼容 docs/ 与 harness/ 两类项目,通用 + 尊重项目约定,确认点吸收探测的不可预测性。理由:skill 通用性要求不假设目录,本仓库实践要求 harness/,三层机制同时满足;探测单独用太魔法(B 缺点)、声明单独用太重(C 缺点),组合 + 确认是两者优点的交集

- ✍️ 自定义: __________所有design Q相关的文件称为harness文件，放在harness文件夹下，docs保留为项目自身文件，与harness文件解耦

### Q1.1 (条件: Q1 选 C 或 E 才答) 声明格式怎么定(skill 需稳定识别项目声明的落盘根)?   [落盘: HLD#文档机制]

<出题依据:若机制含「声明优先」,skill 需稳定解析声明。格式太松会漏读、太严增加项目负担。本仓库 CLAUDE.md 已有「## skill 家族协作」节下的「落盘路径速查表」(自然语言表格),可作天然锚点。>

- [ ]  A. CLAUDE.md 专设固定标题节(如 `## skill 落盘根` + 一行路径) —— 显式稳定;但侵入 CLAUDE.md 结构,为本仓库等多项目加一个强制节
- [ ]  B. HTML 注释标记行(`<!-- skill-sink-root: harness/ -->`) —— 不影响渲染;但需 skill 解析注释,且注释对人不透明(维护者看不出这行管什么)
- [ ]  C. AGENTS.md / CLAUDE.md frontmatter 键值(如 `skill-sink-root: harness/`) —— 结构化最规整;但 CLAUDE.md 通常无 frontmatter(只有 SKILL.md 有),强加 frontmatter 不自然
- [ ]  🤔 我定不了 → 推迟/降风险
- [ ]  D. 不强制格式,skill 自然语言识别 CLAUDE.md 中「落盘根 / 落盘速查 / harness 区 / 设计产物落」等关键词 + 落盘前确认兜底 ★推荐 —— 零侵入:本仓库 CLAUDE.md「落盘路径速查表」自然命中,无需改 CLAUDE.md;关键词命中不到时回落探测 + 确认(Q1 E 的二三道防线);确认点兜底误识别。理由:CLAUDE.md 是写给人读的,强制机器格式破坏可读性;自然语言识别 + 确认 = 人机都舒服

- ✍️ 自定义: __________

## Q2. 改 skill 规格的「全局生效」如何留痕?(W00 第 13 条)   [落盘: design-Q DESIGN.md + VISION#风险]

<出题依据:改 `~/.claude/skills/design-questionnaire/` 立即影响所有项目下次调用 design-Q。可回退(文件可改回)但影响面广。W00 第 13 条默认「记风险不立 ADR」未采纳。skill 物理在 `~/.claude/`(用户级),不在本仓库;本仓库是方法论仓库,skill 是其执行体。>

- [ ]  A. 立本仓库 ADR(记「skill 规格变更全局生效」架构事实) —— 影响面大值得 ADR 留痕;但 skill 在 `~/.claude` 非本仓库(跨域),且可逆性勉强达 ADR 门槛(难逆转性不足——文件可改回)
- [ ]  B. 记本仓库 OD(待决 + 重访触发 = 「某项目因 design-Q 改动产出异常」) —— 灵活;但本质是 skill 的既定特性(全局生效)非「待决事项」,OD 语义是「推迟的决策」不太匹配
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  C. 记 design-Q DESIGN.md 决策表(标「全局生效 / 可回退 / 双向门」) + 本仓库 VISION#风险一句话提及 ★推荐 —— DESIGN.md 是 skill 自身设计决策的正当归处(D1-D22 先例),可逆性不足 ADR 三条件故不立 ADR;本仓库 VISION 风险节提一句(因本次设计产物落本仓库),不跨域立 ADR。理由:留痕归位——skill 决策归 skill 的 DESIGN.md,仓库设计归仓库的 VISION,各守其位

- ✍️ 自定义: __________

---

## W00 处理后追加区(W00 留空项已转为上文 Q1 / Q2)

> W00 第 3 条 → Q1(+ Q1.1);第 13 条 → Q2。

---

## 补充声明

<任何想补充的话:新需求、格式反馈、范围调整、临时想到的风险……没有就留空。agent 处理时必读>

---

## 处理报告摘要(2026-08-06 · W01 → processed)

- **Q1**:未勾选项,✍️ 自定义「所有 design Q 相关的文件称为 harness 文件,放在 harness 文件夹下,docs 保留为项目自身文件,与 harness 文件解耦」→ 方向清楚但「项目无 harness/ 怎么办」未覆盖 → AskUserQuestion 澄清 → **方案 R 采纳**(默认 `harness/` + CLAUDE.md 声明覆盖 + 落盘前确认);自定义原文逐字保留于本文件 Q1 ✍️ 行
- **Q1.1**:条件题(Q1 选 C/E 才答),Q1 经澄清定为 R(含声明覆盖)→ 由 R 推导 = **D**(自然语言识别 CLAUDE.md 关键词 + 确认兜底,零侵入,与 R 的确认机制契合),不再单独追问(避免过度问卷化);若用户对声明格式有异议可在闸门提出
- **Q2**:[x] **C 采纳**(记 design-Q DESIGN.md 决策表 + 本 VISION 风险一句话,各守其位,不跨域立 ADR)
- **opt-in 关,无取消率;无逃生舱**
- **VISION.md 已落** `harness/design/skill-spec-revamp/VISION.md`(方案 R 第一次落地:本仓库声明命中 harness/)
- **vision 阶段骨架 V1–V6 全覆盖,动态盲点清零**
