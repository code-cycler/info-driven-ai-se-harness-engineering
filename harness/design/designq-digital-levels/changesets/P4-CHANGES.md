# P4(引擎四副本 stage 枚举 LN 化)变更清单

- 四 skill(两侧)QUESTIONNAIRE-FORMAT:frontmatter/模板 stage 枚举 `vision | hld | lld` → `L0-vision | L1-contract | L2-build | <自声明层名>(旧名别名)`;命名规则 `stage ∈` 行同步;grill-Q 头部注同步。
- PROCESSING-RULES:无 stage 枚举词汇,免改(grep 0)。
- action-Q:FORMAT 无旧枚举(OD-11 有意分叉已免疫),仅补 superseded 行对齐。
- 连带补 P0 遗漏:grill/retro/action 项目版 FORMAT 补 2026-08-14 superseded 作废分支行(F027 时仅同步 design-Q,P0 范围外遗漏)。
- 终验:四 skill FORMAT 两侧完全一致;旧枚举 grep 清零。
