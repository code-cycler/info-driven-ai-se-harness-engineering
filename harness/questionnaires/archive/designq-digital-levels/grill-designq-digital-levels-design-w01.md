---
mode: init
wave: 1
stage: grill
created: 2026-08-14
status: processed
---
# 问卷 grill W01 · designq-digital-levels 设计套压测(VISION + HLD,进 lld 前)

> 填写规则:
>
> 1. 每题勾选 `[x]`;默认单选,标「(多选)」可勾多个,选项数不限
> 2. ★ = 推荐选项,附推荐理由;**选项排序 = 非推荐在前 → 逃生舱倒数第二 → 推荐最后**(规则 13)
> 3. 每题末尾 🤔 是逃生舱:勾了 = 我定不了 → agent 走降风险协议,绝不重问
> 4. 选项都不合适 → 在 ✍️ 自定义 后自由书写
> 5. **预勾选 = opt-in 开关**(默认关)

**压测对象**:designq-digital-levels 设计套([VISION](../../design/designq-digital-levels/VISION.md) + [HLD](../../design/designq-digital-levels/HLD.md)),write→review 闭环,压测后进 lld。
**模式**:代码库绑定(绿地特性,D7「现实」= 三 skill 现状规格 + 全局/项目双副本实测 + 引擎副本原文)。

---

## Q1. 引擎副本「不动」与 stage 枚举矛盾——层级制下问卷 frontmatter/命名全是旧词汇   [落盘: 处理报告 → HLD 修订]

出题依据(D7+D8,已核实原文):① W00 #2 裁决「引擎副本(QUESTIONNAIRE-FORMAT / PROCESSING-RULES)不动」;② 但引擎 FORMAT 规定 frontmatter `stage: vision | hld | lld`(枚举三值)且问卷命名 `<stage>-w<NN>.md`;③ 层级制下 design-Q 的「阶段」变为 L0/L1/L2/自定义层名——stage 将出现枚举外值(L3-xxx),「不动」与枚举冲突;且 OD-8 四副本联动(design-Q 引擎改 = 四份评估)。

- [ ]  B. 引擎完全不动,层名自由填入 stage(枚举注释过时但无害,新值照写)  —— 代价:引擎文档与现实漂移(过时枚举误导读者);FORMAT 里 vision|hld|lld 词汇与新制术语不一致
- [X]  C. 推翻 W00 #2,引擎随层级制修订(stage 枚举改「层名」+ 命名规则改 LN 制)  —— 代价:OD-8 四副本联动(design-Q/grill-Q/retro-Q/action-Q 四份 FORMAT+PROCESSING 同步),改造面显著扩大
- [ ]  🤔 我定不了 → 推迟/降风险
- [ ]  A. 最小豁免修订:引擎只改两处文字——stage 枚举注释扩为「design-Q 层级制下取层名(L0/L1/…);grill-Q/retro-Q 不变」+ 命名规则补一句「design-Q 层级制问卷名 `<LN层名>-w<NN>.md`」;四副本同步仅此两处,其余不动  ★推荐 —— 保住「引擎大体不动」意图,消除枚举矛盾;同步成本一次性两句×四份,可控

- ✍️ 自定义: __________

---

## Q2. 三 skill 双向漂移实锤——「全局=实验/项目=backup」的单向假设已破,HLD 部署流程要改   [落盘: 处理报告 → HLD §6 修订 + OD-24 更新]

出题依据(D7,全量实测):① HLD §6 / 闸门汇报只记录 design-Q FORMAT 一处漂移;② 实测全貌:**long-running SKILL.md 项目版领先**(多 HARNESS-RULES 引用句);**doctor-harness 全局版领先**(HARNESS-RULES 三处 2026-08-14 增补:活跃区子目录/superseded 处置/布局合规;DESIGN/CHANGELOG 亦异)——双向漂移,全局版本身在活跃演进;③ OD-24 假设「项目 = 稳定 backup 基线」不成立:HARNESS-RULES 的全局版比项目版新。

- [ ]  A. 维持 HLD 现流程(backup 全局版 → 改造文件替换 → 回灌时合并)  —— 代价:改造若以项目版为基底改 doctor/long-running,会覆盖掉全局版今日增补(活体回退事故)
- [ ]  B. 推翻 OD-24,改为「全局版 = 唯一活跃开发线,项目版仅随发布门同步」  —— 代价:项目版(公开仓库)滞后于真实演进,回灌语义消失,发布门频率被迫提高
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  C. 双向合并基线:改造前先做一次「全局⇄项目双向同步」(全局版今日增补回灌项目版 + 项目版领先句推全局版,达成一致起点)→ 再 backup → 再改造;OD-24 措辞更新为「双向漂移,任一侧领先时先同步再实验」  ★推荐 —— 消除「哪边是基线」歧义;今日增补(superseded/活跃子目录)本就该进项目版;同步是一次性可核验动作

- ✍️ 自定义: __________

---

## Q3. long-running 铁律「任何时候都不一次处理多个功能」与多 worktree 并行正面冲突,HLD 未提   [落盘: 处理报告 → HLD §4 补冲突处理]

出题依据(D7,已核实原文):long-running SKILL.md §6「**必须严格遵守**:一次只处理一个功能」+ §10 强制执行「**任何时候都不一次处理多个功能**」+ §8 禁止行为表「一次处理多个功能」;HLD §4 多 worktree 并行 = 多功能同时进行。语义可调和(每 worktree 会话内仍一次一个),但 SKILL.md 禁令措辞不改则新旧行为矛盾。

- [ ]  A. 不动禁令,靠 HLD 解释(「每会话一次一个,多 worktree = 多会话各一个」)  —— 代价:SKILL.md 字面禁令与行为矛盾,AI 执行时可能按禁令拒绝并行(规格冲突 = 执行不可预测)
- [ ]  C. 禁令整体重写为「单会话内一次一个功能;多 worktree 模式下并行度由人确认的任务包数决定」  —— 代价:改动大,且「一次一个」作为反半成品护栏的原初语义(Anthropic harness 原则)被稀释
- [X]  🤔 我定不了 → 推迟/降风险
- [ ]  B. 限定式改写:三处禁令改「**单 worktree 会话内**一次只处理一个功能;多 worktree 并行仅在执行模式 + 人确认的任务包边界内」——原初护栏(防半成品)保留在会话粒度,并行合法性显式限定  ★推荐 —— 护栏语义无损迁移;禁令与新行为不再矛盾;改动三句可控

- ✍️ 自定义: __________

---

## Q4. LN 制 → feature_list 重建规则缺失——「从 LLD 阶段拆分反推」在新制下无对应物   [落盘: 处理报告 → HLD §4 补重建规则]

出题依据(D5+D7):long-running §5.3「有 design-Q 产物:读 VISION/HLD/LLD……**从 LLD 阶段拆分反推** feature_list」——全是旧制词汇;新制下:① 文件叫 `LN-<功能>.md`;② 单层交付(L0 only)时没有 L2-build,feature 从哪反推?③ 混合制下 L1+ 职责自声明,阶段拆分内容位置不定。HLD §4 只说「从设计产物重建 feature_list」,映射规则未定义。

- [ ]  B. 固定锚点规则:feature 一律从「L0 验收标准 + 最低构建层的阶段拆分(若有)」反推;单层交付时直接从 L0 验收标准逐条反推  —— 代价:单层 L0 验收标准若写得粗,反推出的 feature 粒度过大(需要 L0 模板预设「验收可拆分」写法约束)
- [ ]  C. 规则留到 lld 阶段再细化,HLD 不写  —— 代价:准备模式是 long-running 改造核心,重建规则缺失 = lld 无据可依
- [ ]  🤔 我定不了 → 推退/降风险
- [X]  A. 分层反推规则(基于 B 补边界):feature 从「最低**构建语义层**(含 L2-build 模板或自声明的构建/阶段内容)」反推;无构建层(单层交付)→ 从 L0 验收标准反推,且 L0 模板加一条写法约束「验收标准按可独立验证的条目写」(与既有 V4 验收可检查原则同源)  ★推荐 —— 与混合制职责模型自洽(构建内容不一定在固定层);单层场景有兜底;写法约束成本一句

- ✍️ 自定义: __________

---

## Q5. 单文件多节形态下 W00 归属未定义;层内波次(W01+)沿用未写明   [落盘: 处理报告 → HLD §2/§3 补]

出题依据(D5):① vision W00 #13「每层独立 W00」——但单文件多节形态(小项目)下,是每节(L0/L1 各)一个 W00 还是整文件一个 W00?② HLD 协议集只写了层闸门,层内波次循环(W01+ 波、覆盖清单、闸门问句)沿用旧制未明说——层制是替换「阶段」还是叠加在「阶段」之上,规格读者会产生两种理解。

- [ ]  A. 每节(层)独立 W00,单文件形态也一样(严格按层)  —— 代价:小项目单文件 3 层 = 3 份 W00 文件,形式主义风险(正是设计原则 1 要防的)
- [ ]  C. 层内波次按旧制沿用不必写(HLD 不重复已有机制)  —— 代价:「层替代阶段还是叠加」歧义留给读者,执行分叉
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  B. 按形态分派:多文件形态 = 每层独立 W00(已定);单文件形态 = **整文件一个 W00**(全层决策默认值合一,按层分节列出);层内波次明确「沿用旧制波次循环(骨架必答项按层取),层只是产物组织维度、阶段词汇统一映射为层」写进 HLD §3 总则一句  ★推荐 —— 单文件形态免于 W00 碎片化(设计原则 1);歧义一句话消除

- ✍️ 自定义: __________

---

## Q6. 全局实验版爆炸半径:实验期间其他项目触发三 skill 会用实验版   [落盘: 处理报告 → OD-24 补边界]

出题依据(D2):全局 `~/.claude/skills/` 是所有项目共享的;改造装载后,**任何项目**的下次 design-Q/long-running/doctor 调用都走新制——其他项目若在旧制预期中(或正在半程),行为突变。HLD/OD-24 未定义实验期其他项目的防护。

- [ ]  B. 不防护,接受(单人使用,实验期短,知道自己在实验)  —— 代价:实验期间恰好在他项目跑 design-Q 会踩未验证新制;若新制有 bug 影响面 = 全部项目
- [ ]  C. 实验版装影子目录(`~/.claude/skills-experiment/`),手动切换启用  —— 代价:影子目录 Claude Code 不自动加载,「实验」需要手动切换动作,便利性差且与「全局作实验」的用户裁决相反
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  A. 接受 + 显式边界:OD-24 补「实验期约束——① 实验窗口内避免在其他项目发起三 skill 的正式流程(非正式使用自担);② 实验期发现 bug 优先修复而非回滚(除非破坏性);③ DOGFOOD 完成即回灌收口,窗口目标 ≤ 2 周」  ★推荐 —— 尊重「全局作实验」原裁决;风险显式声明而非默认无人知晓;窗口上限防实验无限期化

- ✍️ 自定义: __________

---

## Q7. harness-check.py(项目 scripts/)的冻结边界模糊——doctor 改造含脚本扩展,落哪侧?   [落盘: 处理报告 → HLD §6 补]

出题依据(D2+D7):① HLD §6「本项目 skills/ 实验期冻结」——但 doctor 改造三件套之一是 harness-check.py 扩展(LN 命名校验),脚本在**项目 scripts/**(全局侧无此脚本);② 冻结例外只写了「确凿修正类」;③ 实验期若不扩脚本,DOGFOOD 案例的 LN 布局无法机器校验(校验缺失 = 验收 ④ 打折)。

- [ ]  B. 脚本等回灌再改,实验期校验全人工  —— 代价:DOGFOOD 两案例的 LN 命名/层级校验纯手工,验收 ④(反简化+最小必含核对)负担重且易漏
- [ ]  C. 脚本属项目代码不走 skill 冻结,直接改  —— 代价:「冻结」边界出现口子;且旧三件命名豁免若实现不当,harness-check 会对**存量设计套**(VISION/HLD/LLD 裸放)误报
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  A. 脚本列入 doctor 改造包随全局实验节奏:实验期在**项目 scripts/ 实现并自测**(工具脚本非 skill 行为面,不属于冻结的本意),但合入即带「豁免清单:存量 VISION/HLD/LLD 命名合法至迁移完成」——DOGFOOD 校验可用,冻结语义(skill 行为面)不破  ★推荐 —— 冻结的本意是 skill 行为稳定,脚本是其配套工具;豁免清单防误报;实验期即可验证校验逻辑

- ✍️ 自定义: __________

---

## Q8. 多 worktree 代码级合并冲突——任务包防契约冲突,防不了两线程改同一文件   [落盘: 处理报告 → HLD §5 补合并策略]

出题依据(D4):HLD §5 边缘场景表有「契约冲突」(任务包边界 + 上层契约优先 + 人裁定),但**代码级**合并冲突(两线程改同一文件的不同部分)无策略;主会话「管合并顺序」只有一句——顺序定了,冲突来了怎么办未写。

- [ ]  A. 冲突时人裁定(现状语义,冲突罕见靠人)  —— 代价:冲突处理无预设规则,每次临场;频繁冲突时人成为瓶颈(与多线程省时的初衷相悖)
- [ ]  C. 全局锁:任务包声明「文件所有权」,每文件归属单线程,越界改动禁止  —— 代价:所有权声明成本高(要求准备模式就预知文件级改动面——对未开工的实现预测过细,形式主义);跨文件重构被锁死
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  B. 三层防线:① 准备模式任务包尽量按模块边界切线程(源头上减少同文件交叉);② 合并顺序按依赖链串行(上游线程先并);③ 真冲突(git merge 冲突)停在该线程,人裁定后继续——不预设文件所有权,不承诺零冲突,冲突是小概率且有明确处置路径  ★推荐 —— 与设计原则 1(实用)一致:不过度设计;三层每层成本低;冲突处置路径明确

- ✍️ 自定义: __________

---

## Q9. L0 自检信号⑤「AI 判定构造细节不足以直接实现」= AI 自评——撞哲学 L3 黑盒警戒线   [落盘: 处理报告 → HLD §3 修订]

出题依据(D1):自检信号①–④客观可数(模块数/依赖类数/feature_list/验收门槛),⑤是「AI 判定」——方法论自家哲学 §4.1 L3「AI 自评不能单独证明通过」+ CONTEXT 判别表「AI 自称『只是优化』但影响范围不明 → 存疑按判断性处理」。AI 说「细节够/不够」恰是自评场景。

- [ ]  B. 删⑤,只留客观四信号  —— 代价:四信号覆盖不到「结构简单但实现路径有深坑」的场景(如算法选型未定),自检漏报
- [ ]  C. 保留⑤原样(AI 判定有效,哲学管的是「验证结论」不是「触发建议」)  —— 代价:术语上可辩护,但执行上 AI 倾向于「觉得能实现」→ 漏报细化需求,与 (a)(b) 盲区里「AI 不会说不知道」同根
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  A. ⑤改「AI 判定 + 人闸门确认」双签:AI 在 L0 闸门给出五信号命中清单(⑤命中时必须附「哪些构造细节缺失」的具体理由),人在闸门确认——AI 判定只是触发建议,增层决策由人拍板(与铁律 1 一致);四信号命中时同样过人闸门(本就是闸门语义)  ★推荐 —— 不删信息(AI 的实现可行性预判有价值)但决策权留人;⑤降格为「建议信号」,与哲学 L3「AI 自评不能单独证明」精确对齐

- ✍️ 自定义: __________

---

## Q10. 验收判据三处不可核验:「100% 映射」「走通」「存量改造完成」   [落盘: 处理报告 → VISION V4 修订]

出题依据(D6):① V4「旧三档坍缩语义 100% 可映射到新制(无丢失)」——100% 如何核对?无清单;②「DOGFOOD 两个案例走通」——走通判据未定义(自检真实触发算不算必验项?);③ HLD §2 存量改造「缺项标待补不代写」——改造完成的 DoD 是「标完即成档」还是「待补全部人工填完」?

- [ ]  B. 判据留 DOGFOOD 时临场定  —— 代价:验收时各说各话;「走通」容易被「跑完了」替代(形式主义 dogfood 失败模式的变体)
- [ ]  C. 全部量化(映射表条目数 grep 核对 / 走通 = N 个信号触发 / 改造 = 100% 待补填写)  —— 代价:伪精确;映射表本身是实施产物,验收前无法预知条目数
- [ ]  🤔 我定不了 → 推迟/降风险
- [X]  A. 可核验化三句:①「100% 映射」= 迁移映射表(doctor 产出)逐行有「旧语义 → 新落点」且旧三档每一档的产物字段在总览表中有对应列,人工逐行核对打勾;②「走通」= 案例产生 LN 产物 + L0 自检至少一次真实触发并留处理记录 + 案例人确认可用;③ 存量改造 DoD = 「标完即成档」(改造的交付是结构 + 缺口标注),待补项进 TODO 由人后续填——不把「人填完设计」算进改造验收  ★推荐 —— 三处判据都可执行可留痕;③ 与「不代写」自洽(改造交结构,设计归人)

- ✍️ 自定义: __________

---

## 补充声明

<任何想补充的话:压测范围调整、其他设计疑点……没有就留空。agent 处理时必读>在完成本次更改前必须保存并留存信号，及时作retro

---

## 处理摘要(2026-08-15)

10 题全答,Q3 逃生舱(降风险:双向门采推荐 B + provisional + 重访触发 = 多 worktree 首次真实使用复核);Q1 用户推翻推荐选 C(引擎随层级制修订,推翻 W00 #2,OD-8 四副本联动);其余 8 题选 ★推荐。补充声明 1 条:改造完成前过程信号即时保存留痕 + 及时 retro。
**执行**(用户授权「执行全部+进 lld」):VISION(范围改/验收可核验化五条/过程约束节)+ HLD 六处(§2 引擎修订与 W00 分派、§3 总则+⑤双签、§4 反推规则+禁令调和 provisional、§5 代码级冲突三层防线、§6 双向同步+脚本边界+信号保存、§8 被否决项 +2)+ OD-24(双向漂移措辞+实验期三条)。
