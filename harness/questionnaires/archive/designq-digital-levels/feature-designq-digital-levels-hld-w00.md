---
mode: feature
wave: 0
stage: hld
created: 2026-08-14
status: processed
---
# 问卷 feature-designq-digital-levels hld W00 · Preview(决策默认值 yes/no 速答)

> **本波是 preview 预答层**(独立 wave 0):把 AI 有明确默认倾向的决策点逐条列出,人只做 yes/no 速答。
>
> **作答规则**:
>
> - **预勾选 = opt-in 开关**(默认关):仅当你启动本 skill 时明确说「预勾选」,要点才预勾 `[x]`;未启用时全部 `[ ]`,人逐条作答
> - **取消勾选(留空)= 不采纳** → 该要点转入 W01 单独拷问(出选项深究)
> - **单向门要点(发布 / 删除 / 花钱 / 脱敏等不可逆)永不预勾**,强制人逐条显式勾选确认
> - 本波**不用 🤔**(yes/no 二选一,无中间态);真定不了 → 取消勾选即可,转 W01 深究
> - 若「大体同意但要改一两处」→ 取消勾选,转 W01 时在深究题里给自定义值

**hld 阶段目标**:把 VISION 定案的机制(17 要点 + 4 设计原则)转成**可执行的架构规格**——层骨架结构、命名/模板、契约与协议、doctor 拓展、部署回灌。对应骨架 H1 架构 / H2 选型 / H3 契约 / H4 运维 / H5 ADR 识别。

## 决策默认值清单

**骨架与命名(H1)**

- [X]  **1 STAGE-SKELETONS 新结构**:重写为四部分——① 层级骨架总则(混合制职责模型 + 命名规范);② **最小必含总览**(单表:层 × 最小必含 × 反简化锚点,置于文件顶部,设计原则 3);③ 层职责模板库(L0-vision 固定 + L1-contract / L2-build 默认模板 + 自声明机制);④ 协议集(判别/裁决/插层/回退/自检) 〔VISION 机制要点 + 设计原则 3〕
- [X]  **2 文件命名规范**:`L<N>-<功能>.md` 数字-功能式——`L0-vision.md`(恒在)/ `L1-contract.md` / `L2-build.md` / `L3-<自定义功能>.md`;一眼可识功能(设计原则 + W01 定案「L0-vision」);目录 `harness/design/<feature>/` 〔VISION 核心场景〕
- [X]  **3 每文件头全局视野**:每个 LN 文件开头固定导览块(四行:本层位置与职责 / 覆盖范围 / 上下游层依赖 / 契约项声明)——设计原则 4 的规格化;单文件多节形态则在文件头做一个全层导览 〔设计原则 4 + 对标仓库 L1 阅读指南参照〕
- [X]  **4 单文件多节形态规范**:小项目单文件内 `## L0-vision` / `## L1-…` 节标题分节;L0 自检节同样内置;节间裁决与文件间同规 〔W00 #10 + 设计原则 2 边缘场景〕

**协议规格(H3,硬约束)**

- [X]  **5 层间判别落盘格式**:每条设计决策标注所属层(判别三问的结果随层文件落盘),跨层引用用「见 L1-契约#锚点」式——保证「一条决策一个家」 〔W00 #9 细化〕
- [X]  **6 裁决规则双落点**:「上层契约优先于下层细节」同时写进 ① STAGE-SKELETONS 协议集(总则)② 每个层文件头部导览的「契约项声明」行(本层哪些项是下层硬约束)——总则管规则、声明管实例 〔W00 #8 细化 + Q1-C〕
- [X]  **7 插层协议规格**:触发信号(深度不足:实现受阻 / 契约需细化 / 新子系统)→ 插层位置判定(默认底层追加;确需中间层时,新层继承上层契约并重过闸门)→ 新层独立 W00 → 处理报告留痕(触发原因 / 影响面 / 受影响下层清单) 〔W00 #7 细化 + 设计原则 2〕
- [X]  **8 阶段回退协议规格**:上游失效 → 暂停下游 → 标注失效项(作废/仍有效二分)→ 回上游补波 → 只重过受影响层闸门;已归档问卷不动,回退事实记处理报告 〔W01 Q2-C 落地〕
- [X]  **9 L0 自检信号清单**:L0 收尾闸门内置自检节,触发细化的信号——① 多模块/多子系统;② 外部依赖 ≥ 2 类;③ 跨会话实现(需 feature_list);④ 验收标准含性能/安全门槛;⑤ AI 判定「构造细节不足以直接实现」。命中任一 → 增层;全不命中 → 单层交付合法 〔W01 定案细化 + 设计原则 1(实用性:信号可判定)〕

**doctor-harness 拓展(H1/H3)**

- [X]  **10 doctor 拓展载体**:层级治理规则落 [HARNESS-RULES.md](../../skills/doctor-harness/HARNESS-RULES.md) 增补节(层级布局规则 + 存量改造流程 + 旧档迁移映射表);doctor SKILL.md 触发词补「层级改造/迁移」 〔W00 #4/#16 + 补充声明〕
- [X]  **11 存量改造流程**:无 harness 文件的项目 → 盘点既有文档(构想/基本设计/散落决策)→ 按语义映射到 LN 结构(最小必含总览为核对基准)→ 缺项标「待补」**不代写**(AI 不替人补设计,只标缺口)→ 人确认后成档 〔补充声明「将无 harness 文件改造为标准结构」+ 铁律 1〕
- [X]  **12 旧档迁移映射表**(doctor 承载,替代原 W00 #16 载体):VISION/HLD/LLD 旧三档 → LN 新制对照(坍缩 hld 档 → L0+L1;坍缩 lld 档 → L0+L2;完整档 → 三层);存量设计套迁移时按表执行,doctor-harness 校验 〔用户补充声明裁决〕
- [X]  **13 harness-check.py 扩展**:新增 LN 命名校验(`L\d+-<功能>.md` 格式)+ feature 目录下层级顺序合理性提示(L1 存在而 L0 缺失 = 违规);旧三件命名(VISION/HLD/LLD)在存量迁移完成前仍合法(豁免清单) 〔设计原则 1 + 渐进迁移〕

**部署与回灌(H4)**

- [X]  **14 全局实验版装载流程**:① backup 全局版(`~/.claude/skills/design-questionnaire/` 全目录 diff 存档到本项目 harness/design/designq-digital-levels/global-backup/);② 替换改造文件;③ 记录变更清单(哪些文件改了什么)——补充声明「全局更改前先 backup」的规格化 〔OD-24 + 补充声明 3〕
- [X]  **15 保留全局版既有增补**:以全局版为基底改造时,保留其独有内容(已实测漂移一处:FORMAT 的「superseded 作废分支」2026-08-14 增补)——回灌时合并进项目版 〔闸门汇报台账 ①〕
- [X]  **16 回灌流程**:DOGFOOD 通过 → 用户确认 → 全局版变更合并回项目版(施加脱敏差)→ OD-24 该轮关闭 → harness-check + 脱敏门验证 〔OD-24〕

**边缘场景与 ADR(H5 + 设计原则 2)**

- [X]  **17 边缘场景清单入规格**:hld 交付物含「边缘场景表」——单层交付时插层如何触发(自检节承担)/ 单文件形态下裁决锚点如何引用(节锚点)/ 插中间层时契约继承 / 跨 feature 复用某层文件(禁止:层文件属 feature,复用走 ADR 引用不复制)——每条给处理规则 〔设计原则 2 规格化〕
- [X]  **18 ADR 识别(本 feature 预判)**:层级制改造整体拟立 1 份 ADR(三条件命中:难逆转——产物结构全变 + 会困惑——旧术语兼容 + 真权衡——固定语义 vs 浮动已辩);「混合制职责模型」与「最小 1 层」作为该 ADR 的决策点写入,不另立 〔H5 + ADR 三条件〕

## 补充声明

<任何想补充的话:对默认倾向的异议、边缘场景补充、协议细节修正……没有就留空。agent 处理时必读>拓宽修改文件范围，本次要涉及design Q、doctor H、long-running-agent 等;LN文件后面跟着的尾缀应该包含其所有主要功能，如L0-vision-……等；design结束后（可能包含grill）应该停下，必须询问是否多需要支持线程同时开工，并修改long-running-agent，以支持多worktree，改为准备模式和执行模式;

---

## 处理摘要(2026-08-14,hld W00 + 小波)

- **采纳**:18/18 全采纳,0 留空,0 转 W01。
- **补充声明 3 条裁决**:① **范围扩容**——long-running-agent 纳入改造(design-Q + doctor + long-running 三 skill);② **LN 命名尾缀多功能**——`L<N>-<功能1>-<功能2>…` 覆盖该层所有主要功能;③ **design 收尾停点 + long-running 双模式改造**(多 worktree 支持)。
- **小波 W01(1 题,AskUserQuestion)**:双模式语义 = **先备后执**——准备模式(不动代码:重建 feature_list + 规划并行线程 + 任务包,人审查确认开工)/ 执行模式(各 worktree 独立会话并行实现,主会话汇总进度 + 管合并顺序);模式切换由人确认。选 ★推荐。
- **落盘**:[VISION](../../design/designq-digital-levels/VISION.md) 范围节与机制要点表扩容(19 + 2 条);[HLD](../../design/designq-digital-levels/HLD.md) 起草(头部导览块以身作则;§1 架构图 / §2 命名模板 / §3 协议集五件 / §4 停点与双模式 / §5 边缘场景表 6 条 / §6 部署回灌 / §7 单 ADR / §8 被否决项 6 条)。
- **设计套自身命名说明**:本套保留 VISION/HLD/LLD 旧命名(新 LN 制是产出不自举);dogfood 时可作迁移演示实例。
