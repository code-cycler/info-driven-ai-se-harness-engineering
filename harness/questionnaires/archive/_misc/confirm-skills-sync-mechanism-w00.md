---
mode: feature
wave: 0
stage: confirm
created: 2026-08-19
status: archived
---
# 问卷 confirm W00 · 细节确认清单（AI 汇报理解，人核对）

> **作答规则**：勾 `[x]` = 理解正确（按该理解执行）；留空 = 有误要改（转正式题/小波深究）；本波不用 🤔。
> 来源标注于〔〕：〔推断〕= AI 填的，重点核对。
> 上下文：上一行动（confirm-action-q-sync，2026-08-18）条目 13 留下的动态盲点——「项目内修、全局漏修」同步空隙；用户裁决「做成机制化」。

## 细节确认清单

### 目标

- [X]  **1 行动定义**：把 skill 双侧同步治理机制化 = 新增校验脚本 + CLAUDE.md 挂接，使「改了一侧、漏了另一侧」可被例行发现，不再依赖人工记忆。〔用户原话「做成机制化」+ 上轮问卷条目 13 记录〕
- [X]  **2 载体与形态（推荐）**：新增 [scripts/skills-sync-check.py](scripts/) ——与 harness-check.py 同风格（python3、纯 stdlib、check-only、0 违规静默、EXIT 码语义）。**不提供 --apply 自动同步**：哪侧内容正确是语义判断（本次事故核心教训——8/14 修订在全局对、归档路径在项目对，自动选边必然错一半），选边永远是人做，脚本只负责「让漂移可见」。〔推断 + 实测：harness-check.py 风格核对；上轮 A/B 型双向合并事实〕
- [X]  **3 检查范围**：repo `skills/` 下每个 skill 目录 ↔ `~/.claude/skills/<同名>/`；逐文件递归对比，报告三类差异——仅项目有 / 仅全局有 / 内容不同。全局侧多出的 ~30 个无关 skill 不检查（不在本仓管辖）。〔实测：repo 9 skill、全局 39 目录〕

### 输入

- [X]  **4 已知例外机制**：`doctor-harness/CHANGELOG.md` 为唯一现存裁决例外（2026-08-18 用户裁决：外部实证条目留全局，双侧不逐字节一致）→ 脚本内置 EXCEPTIONS 白名单，命中项单列输出「豁免（裁决出处：confirm-action-q-sync-w00）」不算违规；**新增例外必须改代码并注明裁决出处**（例外显式、可追溯，不做外置配置）。〔实测 + 用户裁决记录；推断（白名单内置 vs 外置——单用户仓库外置配置过度工程）〕
- [X]  **5 首跑即验证用例**：当前 9 skill 双侧刚同步完（仅该例外）——脚本落地首跑应 0 违规 + 1 豁免，即脚本正确性的现成验证。〔实测：上轮终验 diff -r 0 差异〕

### 输出

- [X]  **6 CLAUDE.md 挂接两处**：①「编辑本仓库的铁律」新增一条（改 `skills/` 或 `~/.claude/skills/` 任一侧后，提交前跑 `python3 scripts/skills-sync-check.py`，与脱敏门并列）；②「唯一工具命令」节改为两条命令（脱敏检查 + 同步检查，标题相应改）。措辞执行时按上下文定稿。〔推断；实测：CLAUDE.md 现有节结构〕
- [X]  **7 状态节历史注记**：CLAUDE.md「仓库状态」加一行（2026-08-19 双侧同步机制化 + 指向归档问卷）。〔推断（仓库惯例：状态节逐事件留痕）〕
- [X]  **8 Git 提交**：conventional commit（如 `feat(scripts): ...`），信息我拟、你审后提交；不推送。〔仓库惯例；上轮同款流程〕

### 约束

- [X]  **9 纯 check-only**：脚本绝不修改任何文件（含不提供 --apply，理由见条目 2）；只读对比 + 报告 + EXIT 码。〔推断〕
- [X]   **10 不动 9 skill 内容与全局侧文件**：本次纯加机制，不改任何 skill 文件；doctor-harness 的 CHANGELOG **不需要**动（本脚本治理 repo 工程脚本层，非 harness/ 区组织规则，HARNESS-RULES「改规则须同步 CHANGELOG」不适用——HARNESS-RULES 管 harness 区分层/迁移/归档，本脚本在区外）。〔推断，重点核对〕

### 边界

- [X]  **11 不做自动选边/自动同步**（同条目 2，边界声明：机制只到「发现」，处置是人的决策——铁律 1）。〔skill 铁律〕
- [X]  **12 不入 push 前三道门强制清单**：本次挂「提交前例行」（条目 6）；是否升格为发布门强制项，留后续使用数据再定（避免一次到位过度收紧）。〔推断〕
- [X]  **13 问卷归档**：处理完移 `archive/_misc/`（与上轮同惯例）。〔惯例〕

### 依赖

- [X]  **14 无外部依赖**：纯本地文件对比，无网络/库/工具链。〔推断〕

## 补充声明

（第四类「用户先验结论」如涉及请填此；亦可用于留空项纠偏）

---

## 处理报告摘要(2026-08-19)

- **confirm-list 统计**:确认正确 14 / 留空纠正 0——全确认,一轮终结,无正式题波、无小波。
- **执行**:① 新增 scripts/skills-sync-check.py(python3 纯 stdlib,check-only 不选边不提供 --apply;三类差异报告「仅项目有/仅全局有/内容不一致」;EXCEPTIONS 白名单内置,doctor-harness/CHANGELOG.md 注明 2026-08-18 裁决出处;例外项两侧重新一致时提示移除白名单;EXIT 0/1 语义);② CLAUDE.md 三处挂接——铁律第 8 条(任一侧改 skill 后提交前跑,0 违规才提交)、工具命令节「唯一」改「两条」(+ skills-sync-check)、状态节加 2026-08-19 机制化注记(连带补记 8-18 同步行动 4fece7c);③ doctor-harness CHANGELOG 按条目 10 不动(harness 区管辖外)。
- **验证**:首跑 0 违规 + 1 豁免(EXIT=0,条目 5 预期命中);反向用例——临时在全局 grill/SKILL.md 追加 1 行,脚本检出「内容不一致」EXIT=1 ✓;实验后还原(首用 rstrip 误剥原有行尾 \n,改用项目版 git-clean 基准 cp 恢复),复验 EXIT=0。终验三脚本全 0:skills-sync-check 0 违规 / desensitize 0 命中 / harness-check 0 违规。
- **异常**:反向用例还原首次失败(rstrip 语义误用),已用项目版基准恢复并复验——教训:实验性写全局侧文件后应以 git-clean 侧为基准恢复,不用字符串操作。
- **覆盖度**:六要素全覆盖;无动态盲点新增(条目 12「是否升格发布门」留使用数据,已有注记);无逃生舱。
- **产物**:scripts/skills-sync-check.py + CLAUDE.md 三处;git 改动待提交(提交信息另呈用户审)。
