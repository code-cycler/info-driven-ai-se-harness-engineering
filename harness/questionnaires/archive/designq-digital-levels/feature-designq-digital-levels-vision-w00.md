---
mode: feature
wave: 0
stage: vision
created: 2026-08-14
status: processed
---
# 问卷 feature-designq-digital-levels vision W00 · Preview(决策默认值 yes/no 速答)

> **本波是 preview 预答层**(独立 wave 0):把 AI 有明确默认倾向的决策点逐条列出,人只做 yes/no 速答。
>
> **作答规则**:
>
> - **预勾选 = opt-in 开关**(默认关):仅当你启动本 skill 时明确说「预勾选」,要点才预勾 `[x]`;未启用时全部 `[ ]`,人逐条作答
> - **取消勾选(留空)= 不采纳** → 该要点转入 W01 单独拷问(出选项深究)
> - **单向门要点(发布 / 删除 / 花钱 / 脱敏等不可逆)永不预勾**,强制人逐条显式勾选确认
> - 本波**不用 🤔**(yes/no 二选一,无中间态);真定不了 → 取消勾选即可,转 W01 深究
> - 若「大体同意但要改一两处」→ 取消勾选,转 W01 时在深究题里给自定义值

**feature 背景**(grill-Q methodology-improvement W01 Q3-A + 自定义 + 补充声明,2026-08-14 已裁决):将 design-Q 的 VISION/HLD/LLD 三时间阶段产物改造为**数字层级 design(第 0 层 / 第 1 层 / 第 2 层……)**,层数按项目大小长短动态调整,同一任务中可动态增层;参照同级对标仓库(Level 1/2/3 = 阅读视角可独立维护,非时间阶段)。执行路径:改造落全局实验版 → DOGFOOD 实测 → 回灌本项目(OD-24)。

## 决策默认值清单

**阶段与范围**

- [X]  **1 阶段档位**:本 feature 走完整 vision→hld→lld 三阶段(结构性 skill 改造,非小修;坍缩会丢契约与迁移设计) 〔SKILL.md 阶段坍缩判别〕
- [X]  **2 改造文件边界**:改造对象 = STAGE-SKELETONS.md 重写 + SKILL.md 定模/坍缩/闸门节修订;**引擎副本(QUESTIONNAIRE-FORMAT / PROCESSING-RULES)不动**(无阶段骨架内容,避免 OD-8 四副本联动) 〔W01 Q10-B 裁决 + OD-8〕
- [X]  **3 skill 波及面**:只改 design-Q;grill-Q / retro-Q / action-Q / doctor-harness 本 feature 不动 〔W01 Q3-A 范围〕
- [X]  **4 doctor-harness 拓展纳入**:doctor-harness 的层级文档布局校验拓展**纳入本 feature 范围**(作为 lld 阶段一项,与主改造同套设计同窗口验收);不另立项 〔补充声明「拓展 doctor 任务范围」+ 避免二次立项成本〕

**层级语义(V3 核心场景)**

- [ ]  **5 层级命名与基数**:层级命名 = **L0 / L1 / L2 …(第 N 层 design)**;L0 = 目标层(原 VISION 语义:目标/范围/场景/验收/风险),L1 = 契约层(原 HLD 的稳定架构子集:模块边界/接口契约/全局选型/ADR),L2 = 构建层(原 LLD:阶段拆分/详细设计/DoD/依赖);L3+ = 深化层(大项目按需) 〔用户自定义「第 0 层 design、第 1 层 design……」+ lens 三层参照〕
- [ ]  **6 层数动态判定**:定模时按「规模 × 经验」判初始层数——小项目(skill/脚本)= 2 层(L0+L2,契约并入 L0 或 L2 标注);中项目 = 3 层(L0/L1/L2);大项目/多子系统 = L3+ 按子系统或深化需要加层;判据写进 SKILL.md 定模节 〔W01 Q3-A「面向大小长短项目动态适配」〕
- [X]  **7 动态增层协议**:实现期发现某块设计深度不足 → **插层**(在现有层之下新增子层或深化层),不回退不重构——插层须过该层闸门 + 处理报告标注触发原因与影响面;与阶段回退协议(W01 Q2-C,已定)互补:回退管「上游失效」,插层管「下游不够深」 〔W01 补充声明「同一任务中也能动态调整」〕
- [X]  **8 层间裁决规则**:STAGE-SKELETONS 明文写入「**上层契约优先于下层细节**」——L(n) 契约是 L(n+1) 的硬约束;下层想改上层契约必须走 ADR-0021 治理性偏差路径(暂停 → 更新上层 → 再继续) 〔W01 Q1-C 已裁决,规格落地于此〕
- [X]  **9 层间归属判别**:现「HLD/LLD 判别法则」升级为「层间归属判别」三问:① 随实现推进会变吗(会 → 越往下层);② 是边界契约还是内部细节(契约 → 契约层);③ 是目标/验收还是手段(目标 → L0) 〔STAGE-SKELETONS 判别法则 + W01 Q1〕

**产物与流程组织**

- [X]  **10 产物文件组织**:数字层级支持两种物理形态——**多层多文件**(每层一文件:`harness/design/<feature>/L0.md / L1.md / L2.md`,大项目)与**单文件多节**(小项目一层文档内按 L0/L1/L2 分节,最小必含按节核对);定模时选定,中途增层可切换形态 〔Q3-B 单文档档被 Q3-A 吸收:数字层级制下单/多文档是形态参数而非档位〕
- [X]  **11 L0 恒在**:无论层数多少,L0(目标/范围/验收)**所有项目必答**——验收标准缺失 = 无限打磨,不可坍缩 〔现坍缩档强制 VISION 的精神保留〕
- [X]  **12 最小必含随层迁移**:现 H1–H5 / L1–L5 最小必含清单**按语义重新分配到各层**(契约类 → L1;构建类 → L2),反简化声明(「约束内容非仅结构」)整体保留并适用于全部层 〔STAGE-SKELETONS 反简化声明〕
- [X]  **13 W00 preview 与层级兼容**:每层仍各出独立 W00(决策默认值清单按层生成),不改为全局单 W00——preview 的价值在「本层将盘问什么」的预览,按层粒度才有效 〔SKILL.md W00 机制〕
- [X]  **14 闸门节奏**:每层过完自身波次后过一次层闸门;层与层之间顺序仍自上而下(L0 → L1 → L2),但**已过闸门的层因插层/回退重开时,只重过受影响层**(非全链重跑) 〔W01 Q2-C 回退协议 + 要点 7〕

**验收与风险(V4/V5)**

- [X]  **15 验收标准(DoD)**:① DOGFOOD 实测两个案例走通(小项目 2 层 + 中项目 3 层,含一次动态插层);② 全局实验版装载且本项目 backup 未动(OD-24);③ 旧三档坍缩的语义 100% 可映射到新制(无丢失);④ STAGE-SKELETONS 反简化声明与最小必含全量核对通过 〔OD-24 + W01 Q7 dogfood 联动〕
- [X]  **16 旧档迁移映射**:坍缩 hld 档(没经验)→ 2 层(L0 + L1,实现中边做边调);坍缩 lld 档(小项目)→ 2 层(L0 + L2);完整档 → 3 层;迁移映射表写进 SKILL.md 定模节,旧术语(VISION/HLD/LLD)保留为各层别名以兼容既有引用 〔SKILL.md 坍缩节 + 术语兼容〕
- [X]  **17 实验期冻结**:本 feature 实施期间(全局实验版验证前),本项目 skills/design-questionnaire/ 一字不改(backup 语义);改造版所有变更只落全局 `~/.claude/skills/design-questionnaire/`,DOGFOOD 通过后一次性回灌 〔OD-24〕

## 补充声明

<任何想补充的话:对层级语义的修正、对默认倾向的异议、补充场景……没有就留空。agent 处理时必读>design文档的多层级划分不严格限定每个数字层级的职责，如小微项目或作小feature、小action可以只有L0，按需细化，在L0规定职责；命名方式要让人一眼看出功能，如L0-vision等；演进skill要谨慎，在全局更改前也先作backup，记录更改以便回退；第16点修改进doctor harness；doctor harness新增将无harness文件（可能有基本构想、基本设计等）改造为当前方法论的标准结构；

---

## 处理摘要(2026-08-14,W00 + 小波 W01)

- **采纳**:15/17(要点 1–4、7–17);**留空转 W01**:2(要点 5 层级命名与基数、要点 6 层数动态判定)。
- **小波 W01(2 题,AskUserQuestion)**:层级职责模型 = **混合制**(L0 固定目标层 `L0-vision`,L1+ 浮动自声明 + 常用模板);层数判定 = **最小 1 层 + L0 自检**(单层交付合法;L0 闸门内置细化自检:多模块/外部依赖/跨会话实现等触发信号)。两题均选 ★推荐。
- **补充声明 4 条裁决**:① 层级职责不严格限定(→混合制吸收);② 命名一眼可识 `L0-vision` 式(→定案);③ 全局更改前先 backup + 记录 diff(→OD-24 强化 + VISION 约束);④ 要点 16 迁移映射表载体改 doctor-harness + doctor 新增「无 harness 文件项目的存量结构改造」职责(→VISION 范围节)。
- **落盘**:[VISION](../../design/designq-digital-levels/VISION.md)(V1–V6 全覆盖,机制要点表 17/17 定案)。
- **未验证信息台账(闸门汇报)**:① 全局实验版与项目版 design-Q diff 实测——SKILL.md 一致;**QUESTIONNAIRE-FORMAT.md 存在实质漂移一处**(全局版多 2026-08-14「superseded 作废分支」增补,项目版缺失)——hld 以全局版为基底时须保留该增补,回灌时合并;② DOGFOOD 项目可用性——实施期验证。

### 补充波(闸门「有补充」,2026-08-14)

用户补充 4 条(逐字):① 「本项目要强调动态调整,避免形式主义,每个功能都要实用」;② 「一般情况下需要考虑边缘场景,这些都是提升 UX 的地方」;③ 「关于最小必含项等,需要有一个总览」;④ 「最好每个文件人从开头就可以有个全局视野」。
处理:4 条均为 vision 级设计原则,直接落 [VISION「设计原则」节](../../design/designq-digital-levels/VISION.md)(约束全部 hld/lld 设计);无新开放问题,不生成补充波问卷。
