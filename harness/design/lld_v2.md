# LLD_v2 · methodology_v3(分阶段实现规格 · grill-Q 压测后修订)

> 本文件是 [LLD_v1](lld_v1.md) 修订版(2026-07-29,grill-Q v3-design [W01](../../harness/questionnaires/archive/methodology/grill-methodology-v3-design-w01.md) 压测 12 题全采纳 + Q3 彻底重排)。
> 修订来源标注(Qn / ADR)。**v1 保留作快照**。
> 上游:[VISION](VISION.md) + [HLD_v2](hld_v2.md) + [ADR-0004](../../harness/adr/0004-methodology-v3-hallucination-thesis.md) / [0005](../../harness/adr/0005-pillar-standard-wording.md) / [0006](../../harness/adr/0006-v3-chapter-restructure.md)。

## L1 阶段拆分

| 阶段 | 内容 | 独立 DoD |
|---|---|---|
| **P0 章节顺序设计**(Q3 新增) | 设计 v3 新章节结构(重排主线),定锚点重写方案 | 章节结构经审,锚点重写映射定 |
| **P1 主文立论重写** | §零 / §一 / §二(机制层 / 双靶子 / 两类断层) | §2.1 四必含 + §2.1 回归 DoD(Q4) |
| **P2 立论挂接** | §5 / §6 / §7(问答 / AI 替代 / 过载)+ §十一 失败模式表(Q6) | 挂接点无断链 |
| **P3 CONTEXT 同步** | 完整术语清单 + 第一支柱定义版 | CONTEXT 与 v3 用词一致 |
| **P4 README/CLAUDE + 发布** | 定义版措辞 + 锚点全重写 + 发布七步 | 全局 DoD 全绿 |

> P0 可先经 grill-with-docs 深究 v3 章节顺序(ADR-0006),再进 P1。

## L2 详细设计(章节级改写规格)

### §2.1 第一支柱(P1)
机制层小节(DoD-1 因果句开篇)+ 三层立论模型表(**含因果方向列**,Q1)+ 保留 120k/400k + 引用过载对策。

### 两类断层(P1,并入新 §一)
人与人断层(挂 AI 替代角色)+ 人与AI断层(挂问答对齐);返工 = 共同症状。

### §5.1 (a)(b) 盲区(P2)
补小节:(a) → preview/Q 捕获、(b) → grill 逼出;原「不替决策 / 帮看清决策」作引子。

### §零 / §一 双靶子(P1)
§零 加 vibe coding 首要;§一 重定位为 vibe coding 失败模式开篇。

### 过载对策链(P2)
补「design-Q 沉淀 → /compact」。

### v3 文件头(P1 起 · Q5 完整谱系)
「v3 相对 v2 演进」+ **完整谱系 v1 单支柱 → v2 双支柱 → v3 补机制层**(不只 v2→v3)。

### v2 文件头(P4)
「历史版本,以 v3 为 current」一行。

### CONTEXT(P3 · Q10+Q11)
完整术语清单(断层/流转/有效上下文/背景缺失 + 信息真空别名 + vibe coding + 三层 + (a)(b))+ 第一支柱**定义版**(ADR-0005)。

### §十一 失败模式表(P2 · Q6 新增)
加 v3 新立论失败模式:① 三层误读(读成同级)② 双靶子失衡 ③ (a)(b) 混淆(用错机制)④ 标准句脱节。

## L3 接口规格(硬约束)

- **§2.1 四必含**:DoD-1 因果句 + 三层表(含因果方向)+ 120k/400k + 过载对策引用——缺一不算 P1 完成。
- **标准措辞**:v3 §2.1 用完整版;CONTEXT/README/CLAUDE 用定义版(ADR-0005);核心子串四处一致。

## L4 DoD(可脚本化 · Q7+Q8 具体化)

### 全局 DoD(实现期跑)
1. **v3 章节完整性**:v3 章节覆盖新立论全部要点(人审 + 章节清单核对)。
2. **标准措辞一致**:grep 核心子串「对抗 AI 在信息真空中的幻觉式自作主张决策」出现在 v3§2.1 + CONTEXT + README + CLAUDE(Q8:用子串不用全句,避加粗干扰)。
3. **[desensitize.py](../../scripts/desensitize.py) 0 命中**。
4. **术语用法一致**:四术语 + 新术语在 v3 + CONTEXT 层级定义一致。
5. **§2.1 回归 DoD**(Q4 新增):grep 确认原「有效上下文」「120k」「400k」「双支柱」关键句仍在 v3 §2.1(防重组误删)。

### 回归 DoD(· Q9 措辞修正 + Q3 重排调整)
~~v1:§4/§6/§7 结构不变~~ → 重排后无「结构不变」;改为 **v3 既有有效内容不丢**:v2 的有效立论(第二支柱 / 5 环节 / Grill / 信息生命周期等)在 v3 中均有着落(章节可重排,内容不丢),人审 + 关键概念 grep 核对。

## L5 依赖与预估

- **依赖**:ADR-0004/0005/0006 + VISION/HLD_v2 已定,无阻塞;纯文档无外部依赖。
- **工作量**:**P0 章节设计 + P1 主文最重**(重排 + 机制层);P2–P4 递减。
- **实现期建议**:long-running-agent 驱动;**P0 章节顺序可先 grill-with-docs 深究**(ADR-0006),feature_list 从本文件 P0–P4 反推。
